import StackIcon from '@mui/icons-material/People';

import StackList from './StackList';
import StackShow from './StackShow';

const resource = {
    list: StackList,
    show: StackShow,
    icon: StackIcon,
};

export default resource;